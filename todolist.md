# WTCS 总控台能力补齐 TodoList

> 出发点：本系统是风洞测控的**唯一总控台**——全部 12 个子系统的控制、协调、参数设置都必须在这里完成，
> 操作员不应该需要再去碰任何子系统本地面板。以下按投产优先级排列，每项含现状与完成标准。
>
> 图例：`[ ]` 待做　`[~]` 部分已有　`[x]` 已完成

## P0 —— 投产前必须具备（安全与基本闭环）

### 1. 全局运行状态机（系统级 Ready 判定）
- [x] 定义系统级状态：`待机 → 准备中 → 就绪 → 运行中 → 停车中 → 急停/安全异常`（`backend/app/services/orchestration.py: compute_system_state`，就绪要求 4 辅机全运行，可用设置 `ready_required_subsystems` 覆盖）
- [x] 总控台顶栏常驻当前系统状态徽章；状态由子系统实时状态聚合（急停 > 安全异常 > 停车中 > 运行中 > 就绪 > 准备中 > 待机）
- [x] 前置条件丢失自动降级：辅机任一停机即从「就绪」降为「准备中」，徽章 title 列出未就绪辅机
- 现状：✅ 已完成（2026-09-23）。状态 10Hz 随遥测推送，大屏/顶栏/总控台同步显示；序列执行/中止均写审计
- 待续：状态机规则表的可视化编辑界面未做（目前由设置项覆盖辅机清单）

### 2. 一键开车/停车序列（联动控制核心）
- [x] 可配置启停序列编排：开车 5 步（冷却水→压缩空气→吹扫风→尾气抽排→主风机）、停车 7 步逆序，定义存库（sequences 表，version 自增留痕），`PUT /api/sequences/{id}` 可改（admin）
- [x] 每步带 wait 条件（running/stopped/point_gt/point_lt/none）+ 超时，失败自动中止并记录原因；急停即时中止
- [x] 总控台可视化：一键开车/停车大按钮 + 执行进度弹窗（步骤 ○◌✓✗— 实时状态，遥测驱动，所有终端同步），可中止；单序列互斥（并发 409）
- 现状：✅ 已完成（2026-09-23）。`backend/app/services/orchestration.py: SequenceEngine`，冒烟 `backend/smoke_sequence.py` 18/18 通过
- 待续：序列步骤的可视化编辑 UI 未做（API 已就绪）

### 3. 联锁规则配置化（安全联锁矩阵）
- [x] 联锁规则可视化配置页：三类规则（报警 / 指令许可拦截 / 自动停车），条件为安全表达式（`main_fan.wind_speed > 30 and rrs.belt_speed < main_fan.wind_speed * 0.8` 式，AST 白名单求值杜绝注入）
- [x] 联锁状态总览：/interlocks 实时真值表（遥测 10Hz 驱动，绿=正常/许可成立，黄=许可不成立或求值错误，红=违规触发，灰=停用），触发项突出显示；违规自动产生产生分级告警（接入告警中心，恢复自动关闭）
- [x] 联锁规则增删改需维护员+，version 自增留痕，每次修改/触发/拦截/自动执行均写审计
- 现状：✅ 已完成（2026-09-23）。引擎 `backend/app/services/interlocks.py`，页面 `frontend/src/pages/InterlocksPage.tsx`，冒烟 `backend/smoke_interlocks.py` 25/25 通过
- 内置规则：路面速度跟随报警、主风机启动许可（辅机不全禁启）、路面启动许可、风速>118 自动停车
- 说明：自动化引擎（序列/程控/流水线）直写适配器不受 block 拦截（可信编排），但 alarm/auto_stop 10Hz 监视一切状态，安全兜底不缺位

### 4. 跨子系统参数联动（设定值协调）
- [ ] 设定风速时自动带出/建议关联设定：路面速度（按速比）、边界层抽吸比、尾气跟随档位——可一键接受或逐项修改
- [ ] 参数下发前置校验：目标值是否在子系统安全限值内、是否与其他运行中子系统冲突
- 现状：ICD 里 `sync_with_fan`（尾气跟随）已有雏形，其余联动全靠人工分别设定
- 完成标准：设定一个风速，相关子系统设定值联动建议自动弹出；冲突参数被拒绝并说明原因

### 5. 命令全生命周期追踪
- [ ] 每条控制命令生成命令单：下发时间、操作员、参数、回执（子系统确认/拒绝）、执行结果、耗时
- [ ] 总控台可见"进行中命令"队列；命令超时/失败自动告警
- [ ] 命令冲突检测：同一子系统 5 秒内的互斥命令（启动 vs 停止）提示并需确认
- 现状：命令发了就发完，只有审计日志，没有回执与结果闭环
- 完成标准：任意命令可在命令历史中查到完整生命周期；仿真适配器也要模拟回执

### 6. 子系统挂牌/维护模式（LOTO）
- [ ] 子系统可置"维护挂牌"状态：禁用所有控制命令（前端置灰+后端拒绝），显示挂牌人/原因/时间
- [ ] 挂牌状态参与全局 Ready 判定（挂牌系统缺失时按规则决定是否可开车）
- 现状：无；维护时只能靠口头通知别动
- 完成标准：挂牌期间任何指令（含 API 直调）被拒绝并写审计

### 7. 试验序列编排（工况自动执行）
- [ ] 把试验流程编排为序列：开车 → 暖机（风速 X 保持 N 分钟）→ 工况 1（参数组+采集）→ 工况 2 … → 停车
- [ ] 与现有试验矩阵/速度曲线打通：矩阵每行展开为序列步骤；执行进度实时可见，可暂停/跳过/中止
- 现状：速度曲线（speed_profiles）只有风速单维度；试验矩阵是数据记录表，不驱动控制
- 完成标准：一个 WLTP 工况试验从开车到停车全自动执行，过程数据自动归属到该试验

## P1 —— 好用、专业（投产后 1 个月内补齐）

### 8. 参数设置面板统一化
- [ ] 把 ICD 中全部 `writable` 点自动生成为各子系统的参数设置面板（带单位、上下限、当前值对比、下发确认）
- [ ] 参数修改二次确认弹窗显示"当前值 → 目标值"对比
- 现状：每个子系统页面只暴露了少数几个设定项，大量 writable 点够不着
- 完成标准：ICD 里标 writable 的点 100% 可在 UI 上设定，且都有审计

### 9. 工况配方（Recipe）管理
- [ ] 跨子系统参数组打包为配方（如"WLTP 工况""标定工况"）：保存、编辑、一键下发、下发前 diff 预览
- [ ] 配方与项目/订单关联，客户门户可见"我的试验用了哪个配方"
- 完成标准：常用工况从"逐个设 6 个参数"变成"选配方 → 确认 → 完成"

### 10. 配置版本与回滚
- [ ] 子系统参数、联锁规则、序列定义的每次修改自动存版本；支持任意两版本 diff、一键回滚
- 完成标准：参数被误改时可查到任意时刻的有效配置并恢复

### 11. 报警阈值与告警规则配置页
- [ ] AI/健康告警的阈值从代码/配置文件中移到数据库，设置页可视化编辑（测点、上下限、持续时间、级别）
- [ ] 告警规则启用/停用开关；屏蔽规则（维护期间临时屏蔽某测点）带自动恢复时间
- 现状：阈值散在后端逻辑里，改阈值要改代码
- 完成标准：操作员在界面上调阈值，1 分钟内生效，全程审计

### 12. 系统健康总览增强
- [ ] 每个子系统健康分（通信、故障、越限、运行时长加权）+ 系统总健康分
- [ ] 可用率统计：按日/周/月统计各子系统可用率、故障次数、MTBF/MTTR（数据已在 equipment_runtime，缺聚合展示）
- 完成标准：大屏和总控台都有健康分；月度报告自动包含可用率章节

### 13. 时间同步监控
- [ ] 各子系统时钟与 NTP 源（config 里已有 `ntp_server` 字段）偏差监控，偏差超阈值告警
- 完成标准：设置页显示各节点时钟偏差；>500ms 告警

### 14. 数据记录策略联动
- [ ] 试验开始时自动启动高速记录、停车自动停止并归档；记录状态（记录中/已归档/存储余量）在总控台可见
- [ ] 存储水位到阈值自动告警并提示清理/转存（遥测与存储页已有基础，缺联动）
- 完成标准：操作员不需要手动管记录开关；存储将满前 7 天有预警

### 15. 命令级权限矩阵
- [ ] 角色权限细化到命令级（如：客户可看演示但不能设风速；操作员可开车不可改联锁）
- [ ] 权限矩阵 UI 增加"命令权限"维度（ICD 的 `min_role` 已定义，需可视化并可配）
- 完成标准：每个命令的最低角色在设置页可调整，保存即生效

## P2 —— 前瞻性（试运行期间逐步落地）

### 16. 多操作员席位与操作权管理
- [ ] 控制权（control token）概念：同一时刻只有一个席位有控制权，其他席位只读；支持申请/移交/强制收回（管理员）
- [ ] 多席位操作互见：谁在控制、谁在看，顶栏显示
- 完成标准：两个浏览器同时登录，控制命令只有持权席位能下发

### 17. 通信中断失效安全策略
- [ ] 每个子系统可配置断链策略：保持/安全停车/仅报警；断链自动触发对应动作并告警
- 现状：断链只有提示，无自动动作
- 完成标准：拔掉某个仿真适配器，系统按配置策略自动响应并留痕

### 18. 历史工况回放
- [ ] 选定历史时间段，按时间轴回放全部测点数据（数字孪生页加载历史数据流），用于事故复盘与培训
- 现状：twin 页只有实时数据
- 完成标准：可拖动时间轴回放任意历史试验全过程

### 19. 开放接口
- [ ] REST API 文档（OpenAPI 已自带，需整理对外说明）+ Webhook（试验完成/报警触发推送企业微信/钉钉）
- [ ] 只读 API Key 体系：给客户/上级单位的数据接口，不暴露控制
- 完成标准：第三方系统可用只读 Key 拉取试验数据；报警可推送到群

### 20. 冗余与高可用（配合投产架构）
- [ ] 双机热备：主备服务心跳、自动切换、状态同步（docs/投产架构规划.md 已有规划，此处跟踪落地）
- [ ] 操作员站双屏/大屏断连自动重连与状态恢复
- 完成标准：主服务宕机 30 秒内备机接管，控制不中断

---

## 建议落地顺序

| 阶段 | 内容 | 理由 |
| --- | --- | --- |
| 第一步 | 1 全局状态机 + 2 启停序列 | 总控台存在的核心价值，先解决"一键开车" |
| 第二步 | 6 挂牌 + 3 联锁配置 + 5 命令追踪 | 安全闭环，真机接入前必须有 |
| 第三步 | 7 试验序列 + 4 参数联动 + 9 配方 | 把试验效率拉起来 |
| 第四步 | 8/10/11/12/13/14/15 | 专业化打磨 |
| 第五步 | 16/17/18/19/20 | 前瞻能力，随试运行逐步上 |

> 每完成一项，在本文档勾选并标注完成日期与对应 commit。
